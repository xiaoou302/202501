import 'package:flutter/material.dart';

/// App color palette based on the design specifications
class AppColors {
  // Main colors
  static const Color midnightBlue = Color(0xFF0A0E1A);
  static const Color stardustWhite = Color(0xFFF0F0F0);

  // Accent colors
  static const Color fieryRed = Color(0xFFFF3B30);
  static const Color coolBlue = Color(0xFF00A9E0);
  static const Color goldenHighlight = Color(0xFFFFD60A);

  // Background colors with opacity
  static Color darkOverlay = Colors.black.withOpacity(0.2);
  static Color cardBackground = Colors.grey.shade900.withOpacity(0.5);

  // Border colors
  static Color borderColor = Colors.white.withOpacity(0.1);
}
