name: chroplexignosis
description: "A cerebral matching game with cyberpunk aesthetics."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # 数据持久化
  shared_preferences: ^2.2.2
  
  # 音频处理
  # 注意：实际实现时需要添加音频库，如 just_audio: ^0.9.36

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  # 资源配置
  # assets:
  #   - assets/images/
  #   - assets/audio/